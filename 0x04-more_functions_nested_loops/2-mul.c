#include "main.h"
/**
 * mul - function that multiplies two integers
 * @a: input 1 for mul function
 * @b: input 2 for mul function
 * Return: multiplcation of two numbers
 */
int mul(int a, int b)
{
int result = a * b;
return (result);
}
