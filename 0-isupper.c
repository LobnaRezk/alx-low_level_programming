#include "main.h"
/**
 * _isupper - function that checks for uppercase character
 * @c: is input for this function
 * Return: returns 1 if c is upper Returns 0 otherwise
 */
int _isupper(int c)
{
if (c >= 65 && c <= 90)
return (1);

else

return (0);

}
