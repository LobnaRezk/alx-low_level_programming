#include <stdio.h>
/**
 * main - prints the number of arguments passed into it
 * @argc: count of char
 * @argv:an arrat
 * Return: 0
 */
int main(int argc, char *argv[])
{
(void) argv;
printf("%d\n", argc);
return (0);
}
