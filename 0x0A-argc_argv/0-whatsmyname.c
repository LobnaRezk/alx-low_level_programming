#include <stdio.h>
/**
 * main - prints program name , followed by new line.
 * @argc: is count of argument ,int
 * @argv: is array of argument, list
 * Return: 0
 */
int main(int argc, char *argv[])
{
int __attribute__ ((unused)) argc;
printf("%s\n", argv[0]);
return (0);
}
